# --- Web framework / API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9
httpx==0.27.2          # required by fastapi.testclient.TestClient (used in tests/)

# --- Computer vision / numerics ---
opencv-python==4.10.0.84
numpy==1.26.4

# --- OCR backends (pretrained, no custom training) ---
easyocr==1.7.2
pytesseract==0.3.13     # also requires the system `tesseract-ocr` binary if used

# --- ML inference runtime (for the ONNX denomination-classifier backend) ---
onnxruntime==1.19.2

# --- Optional secondary UI ---
streamlit==1.38.0
requests==2.32.3

# --- Testing ---
pytest==8.3.3
